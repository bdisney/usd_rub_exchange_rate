<% if @rate.errors.any? %>
  $('.flash-messages').html('<%= j render partial: 'shared/errors', locals: { resource: @rate } %>')
<% else %>
  $('.flash-messages').html('<%= j render partial: 'shared/notice' %>')
<% end %>
$('.form').replaceWith('<%= j render partial: 'form' %>')
